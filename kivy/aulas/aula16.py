# Udemy - 02/28/2017
# Range - Aula 48
"""
Funcao range, gera elementos inteiros
range([start], stop[, step])

ela é bastante usada com for

retorna objeto tipo range, se quiser um list tem que fazer cast
"""

# retorna objeto range
print(range(0, 10, 1))
# faz cast pra virar tipo list
print(list(range(0, 10, 1)))

print(list(range(10)))