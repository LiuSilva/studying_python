# 37: Explique o Colon , quando devo usar ?
"""
O colon (ou dois pontos) é colocado depois do comando for, para indicar ao Python que o proximo bloco que é
 identado pertence ao comando for.
 
Se criar o for, e esquecer de colocar os dois pontos(colon) no final do comando, o Python vai gerar o erro 
de invalid syntax

Assim sempre ao final do comando for, deve ser colocado os dois pontos (colon) como segue

for x in lista: 
    print(x)
    
"""